// const sample = {
//   bearing: -30,
// };

// viewを上書きしたい項目があればここに追記
export const getStatusForUpdate = (layerId: string) => {
  // if (layerId === 'sample_layer') {
  //   return sample;
  // }
  return {};
};
